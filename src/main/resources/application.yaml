spring:
  profiles:
    include: oauth

  datasource:
    # MySQL ??
    # url? ?? MySQL??? ?? ??
    url: jdbc:mysql://localhost:3306/jpa?serverTimezone=UTC&characterEncoding=UTF-8&useSSL=false & allowPublicKeyRetrieval=true
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create # ????? ?? ? ?? ?? (DROP + CREATE)
    properties:
      hibernate:
        #        show_sql: true # Hibernate? DB? ??? ?? ??(DDL, DML)? ????
        format_sql: true # ???? ??? ??? ???

logging:
  level:
    org.hibernate.SQL: debug # ??? ??
    org.hibernate.type: trace # ?? JDBC ????? ????? ??